const SITE_URL =
    process.env.NEXT_PUBLIC_SITE_URL ||
    'https://www.lynktoday.com';

const SITE_NAME =
    'LynkToday';

const DEFAULT_TITLE =
    'LynkToday | Global Trade & Logistics Network';

const DEFAULT_DESCRIPTION =
    'Connect with professionals across global trade, logistics, freight forwarding, customs, shipping and supply chain on LynkToday.';

const DEFAULT_OG_IMAGE =
    `${SITE_URL}/og-image.png`;

export {
    SITE_URL,
    SITE_NAME,
    DEFAULT_TITLE,
    DEFAULT_DESCRIPTION,
    DEFAULT_OG_IMAGE
};
